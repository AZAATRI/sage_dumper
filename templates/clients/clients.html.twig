{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('datatables') }}
{% endblock stylesheets %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('datatables') }}
{% endblock javascripts %}
{% block body %}
    {{ parent() }}
    <div class="container">
        <table id="example" class="table table-striped table-bordered nowrap table-hover" style="width:100%">
            <thead>
            <tr>
                <th>N° Client</th>
                <th>Intitulé Client	</th>
                <th>CT_Contact</th>
                <th>CT_Pays</th>
                <th>CT_Ville</th>
                <th>Code postale</th>
            </tr>
            </thead>
            <tbody>
            {% for client in clients %}
                <tr data-href="{{ path('clients_details',{id : client.Code_Client}) }}">
                    <td>{{ client.Code_Client }}</td>
                    <td>{{ client.CT_Intitule }}</td>
                    <td>{{ client.CT_Contact }}</td>
                    <td>{{ client.CT_Pays }}</td>
                    <td>{{ client.CT_Ville }}</a></td>
                    <td>{{ client.CT_CodePostal }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <script>
        $(document).ready(function() {
            var currentId;
            var table = $('#example').DataTable( {
                responsive: true
            } );
            $('#example tbody ').on('click', 'tr', function () {
                window.location.href = $(this).data('href');
            } );

        } );
    </script>
{% endblock %}